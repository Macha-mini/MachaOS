[package]
name = "machaos-user"
version = "0.1.0"
edition = "2024"
description = "Freestanding test programs loaded by MachaOS as processes"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
codegen-units = 1
strip = true

[profile.dev]
panic = "abort"
opt-level = 1
