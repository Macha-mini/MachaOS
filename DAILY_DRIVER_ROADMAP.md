# MachaOS 普段使い化ロードマップ

日付: 2026-08-06
対象ブランチ: daily-driver

## 0. TL;DR — 現実的な結論

コードベース全体(ハードウェア/ドライバ層・OSコア/ネットワーク/ファイルシステム層・デスクトップ/アプリ層)を
徹底調査した結果、一番大きな発見は **「実機ではほぼ動かない」** ということだった。しかもそれは見た目の
話ではなく、ブート・入力・ストレージ・電源管理という一番土台の部分の話である。

- UEFI非対応(BIOS + GRUB1 + VBEのみ)
- USBスタックが存在しない(PS/2キーボード・マウスのみ)
- NVMe非対応
- AHCI/NICドライバがQEMUの特定デバイスID決め打ち
- ACPI皆無(`shutdown`は実機では何もしない)
- 物理メモリ4GiB上限
- シングルコアのみ

これらを実機で全部解決するのは、詳細は §5 に書くが現実的に見て6ヶ月〜1年以上かかる、今回のスコープ
(数ヶ月)とは別次元の話である。

一方で、QEMU/UTM(macOS上のVM)がデフォルトでエミュレートするハードウェア構成 ——
SeaBIOSレガシー起動・Intel ICH9 AHCIコントローラ・Intel e1000 NIC —— は、MachaOSが既にハードコードで
対応しているものと**ほぼ完全に一致する**。つまり **VMをターゲットにする限り、ハードウェア層は現状でも
概ね動く**。

したがってこのロードマップは以下の前提を置く:

> **普段使いの本命ターゲットは VM(QEMU / UTM / Parallels などをMac上で常用)。**
> 実機ブートアップは別枠の将来課題として §5 に切り出し、このロードマップでは工程化しない。

この前提に立つと、本当に数ヶ月の投資価値があるギャップは **OSコア層(シェル・プロセス管理・
クラッシュ耐性)** と **デスクトップ/アプリ層(コピペ・ファイル操作・画像対応)** に集中している。
これは十分に個人開発で数ヶ月あれば前進できる領域である。

なお、今回のスコープ外として明示的に除外したもの(ユーザーの指示による):
- Webブラウザの実装
- Linux互換層(`linux_abi.rs`)のさらなる拡張を戦略目標にすること(現状の把握のみ行う)

---

## 1. すでにできていること

ネガティブな話ばかりだと実態を見誤るので、先に「ちゃんと動いている」部分を挙げておく。

- **プロセスのフォールト分離が本物**: ページフォルトやCPU例外を起こしたプロセスは、そのプロセスだけを
  kill してカーネル自体はパニックしない (`src/process.rs` の `kill_current`/`kill_current_exception`)。
  自作OSとしてはかなり強い部類の安全網。
- **ゴミ箱機能が本物**: `src/trash.rs` は削除→`/system/Trash`へ移動→復元、が一通り動く「2段階削除」。
  名前衝突の重複回避や、削除前パスの記録(`trash.meta`)まで面倒を見ている。
- **Linux ABI互換層が実際に動く**: `src/linux_abi.rs`(2800行超)は静的/動的リンクのmusl・glibcバイナリ、
  `ld.so`経由の動的リンク、fork/execve/wait4、シグナル配送、poll/epoll/eventfd/timerfd、簡易`/proc`・`/dev`
  まで実装しており、実際にbusyboxのようなバイナリが動く。あくまでLinuxシステムコール表の15〜20%程度
  だが、その範囲では本物。
- **DNS+HTTPが本物**: `wget http://...` はDNS解決込みで実際にファイルを取得できる。
- **今週仕上げたビジュアル層**: 真円の角丸、フォントの読みやすさ、タスクバー/ランチャー/コンテキスト
  メニューの本物の背景ぼかし(アクリル)、ウィンドウドラッグの背景残り修正 —— 見た目の「安っぽさ」は
  概ね解消済み。
- **セッションの位置復元**: 開いていたウィンドウの位置・サイズ・最小化/最大化状態は再起動後も復元される。
- **日本語入力**: ローマ字→かな変換 + 418語の辞書によるかな漢字変換が、メモ帳アプリに限り動作する。

---

## 2. レイヤー別ギャップ一覧

深刻度は **VM上での普段使い** を基準にしている(実機基準ではない点に注意)。

凡例: 🔴 Blocker(daily-driver化を妨げる) / 🟠 High(頻繁に困る) / 🟡 Medium(たまに困る) / 🟢 Low(あれば嬉しい)

### 2.1 ハードウェア/ドライバ層 — VM前提だと大半がLow

| 項目 | 現状 | VM上での深刻度 | 実機での深刻度 |
|---|---|---|---|
| ブート | BIOS/GRUB1/VBEのみ、UEFI皆無 (`src/main.rs:69-193`, `boot/grub/grub.cfg`) | 🟢 QEMU/UTMはレガシーブートで動く | 🔴 UEFI専用機は起動不可 |
| ストレージ | ATA PIO(LBA28のみ)+ AHCIがIntel ICH9(`8086:2922`)決め打ち (`src/ahci.rs:17-18,267`)。NVMe皆無 | 🟢 ICH9はQEMU/UTMの既定AHCIと一致 | 🔴 実機の大半はNVMeで無縁 |
| 入力 | PS/2キーボード/マウスのみ、USBスタック皆無 (`src/keyboard.rs`, `src/mouse.rs`) | 🟢 VMはPS/2エミュレーションが標準 | 🔴 PS/2ポートが無い実機ではキーボード/マウスが使えない |
| ネットワーク | Intel e1000(`8086:100E`)決め打ち (`src/e1000.rs:20-21,156`)。WiFi皆無 | 🟢 e1000はQEMU/UTMの既定NICと一致 | 🔴 実機はWiFi専用や別チップが大半 |
| 電源管理 | ACPI皆無。`shutdown()`はQEMUの`isa-debug-exit`ポート(0xF4)書き込みのみ (`src/power.rs:9-10,25`) | 🟠 VM上でも実際の電源OFFにならず、reboot頼み | 🔴 実機では完全に無効 |
| メモリ | 物理アイデンティティマップ・PMMビットマップとも4GiB固定 (`src/paging.rs:23`, `src/pmm.rs:17-19`) | 🟠 VMに8〜16GiB割り当てても使い切れない | 🔴 実機の大半のメモリが無駄になる |
| マルチコア | SMP皆無、シングルコア専用のロック設計 (`src/task.rs:27-37`) | 🟢 VMを1vCPUにすれば実害なし | 🟡 実機でもコア1つしか使えない(致命的ではない) |

**結論**: VM運用に限れば、電源管理とメモリ上限の2つ以外は今すぐ手を入れる必要はない。

### 2.2 OSコア / プロセス / シェル / ネットワーク層 — ここが本丸

| 項目 | 現状 | 深刻度 |
|---|---|---|
| ゾンビ回収漏れ(実バグ) | `sys_wait4`(`src/linux_abi.rs:822,870`)はゾンビ印を消すだけで`process::reap`を一度も呼ばない。`process::reap`が呼ばれているのは`src/shell.rs`がシェル自身の直接の子プロセスに対して呼ぶ箇所のみで、Linuxプロセスが内部でforkした子(例: busybox shのパイプライン内の各コマンド)は永久にリークする。タスクテーブル(`src/task.rs:176`)は無制限`Vec`なので肥大化し続ける | 🔴 実運用でメモリを食い潰す |
| 未解決のflakyハング | `PHASE7D_HANDOFF.md`(リポジトリ内最新のハンドオフ文書)に、fork+pipe+execve+wait4の統合パスで**タイミング依存の未解決ハング**が明記されている。デバッグビルドでは通り、クリーンビルドでは`sh`が終了を検知できず固まる、と記録されたまま次の一手が引き継がれている | 🔴 パイプライン処理が信頼できない |
| ネイティブシェルにスクリプト機能が無い | `src/shell.rs`の`execute()`は29コマンドのフラットディスパッチャ(`COMMANDS`, `shell.rs:109-113`)。パイプ・リダイレクト・変数・`if`/`for`・コマンド置換が一切ない(`tokenize`は空白分割とダブルクォートのみ, `shell.rs:85`) | 🟠 「シェル」としては使い物にならない |
| `cp`が存在しない | `fat.rs`にコピー関数なし、シェルに`cp`コマンドなし、ファイルエクスプローラーにもコピー&ペーストなし(移動とゴミ箱送りのみ) | 🟠 基本操作が欠けている |
| クラッシュ時にログが残らない | カーネルパニックは`interrupts::halt_forever()`(`src/main.rs:383`)で機械全体が完全停止。診断はシリアル/VGAへの出力のみでディスクには一切残らない | 🟠 シリアルを繋いでいないと原因究明不能 |
| ネットワークスタックが最小限 | TCPは再送・ウィンドウ制御・TIME_WAIT無し(`src/inet.rs:1-27`の自己申告)、DHCP無し(IP固定, `src/net.rs:17,19`)、TLS皆無 | 🟡 ブラウザ非搭載の前提なら実害は限定的 |
| マルチユーザー/ログイン | `src/users.rs`は40行、ユーザーは`macha`固定でパスワードなし、"A multi-user login screen is future work"と明記 | 🟢 個人の普段使いなら意図的に対象外でよい |
| GUI/WM層のテストが皆無 | `make test`はカーネル/プロセス/システムコール層は厚くカバーするが、`src/wm.rs`/`src/desktop.rs`は一切テストされない。GUIの正しさは目視確認のみ(今回のUI修正セッションも全て手動のQEMUスクリーンショット確認だった) | 🟡 見た目のリグレッションに気付きにくい |

### 2.3 デスクトップ/アプリ層

| 項目 | 現状 | 深刻度 |
|---|---|---|
| コピペが行単位のみ | メモ帳のCtrl+C/Ctrl+Xは**現在行まるごと**が対象、「選択モデルがまだ無い」と明記(`src/wm.rs:2461-2464,2477-2480`)。ドラッグ選択・Shift+矢印による範囲選択はどこにも無い。ターミナルは貼り付けのみでコピー自体ができない | 🔴 テキスト編集の基本操作が欠けている |
| 画像フォーマット非対応 | 画像ビューアは独自のヘッダ無しRAW RGB32形式のみ対応(`src/image_viewer.rs:1-3`)。PNG/JPEGのデコードはカーネル内に一切存在せず、`tools/gen_wallpaper.py`が開発機側でPythonのPillowを使って事前変換しているだけ | 🟠 ダウンロードした画像やスクリーンショットがそのまま開けない |
| 音声が皆無 | `audio`/`sound`/`pcm`/`hda`/`ac97`等で全文検索しても実装ゼロ | 🟡 用途次第だが、無音でも致命的ではない |
| ファイルエクスプローラーにコピー&ペースト無し | 移動・リネーム・ゴミ箱送り・複数選択・ドラッグ&ドロップは動くが、コピーができない。再帰検索も無い(現在のフォルダ内のインクリメンタル検索のみ) | 🟠 §2.2の`cp`欠如と表裏一体 |
| GUIアプリの追加にカーネル再ビルドが必須 | 8つのGUIアプリは全て`AppKind`列挙型としてカーネルに埋め込み(`src/wm.rs:152-242`)。CLIツールは`/bin`にELFを置くだけでランチャーに自動出現するが、GUIアプリにはその仕組みが無い | 🟡 開発者本人が使う分には許容範囲 |
| 通知・仮想デスクトップ・ウィンドウスナップが皆無 | `notification`/`toast`で全文検索してもヒット無し。仮想デスクトップ・端へのドラッグでのタイル化も無い | 🟢 QoLの範疇 |
| セッション復元が位置情報のみ | 再起動後にアプリは復元されるが、中身は空で開き直される。メモ帳の未保存テキスト・ターミナルの履歴・エクスプローラーの表示位置は全て失われる(`SessionWindow`に内容を保持するフィールドが無い) | 🟡 QoLの範疇 |

---

## 3. 優先順位付きロードマップ(数ヶ月・個人開発想定)

### Phase A — 出血を止める(バグ修正、目安1〜2週間)

1. **`process::reap`のリーク修正**: `sys_wait4`(`src/linux_abi.rs:870`)がゾンビ状態をクリアするだけで
   なく、実際に`process::reap`相当の解放処理(ページテーブル・物理フレーム・タスクテーブルスロットの
   解放)を呼ぶようにする。今のままではLinux ABI経由の対話利用(busybox shなど)を続けるとメモリが
   確実に枯渇する。
2. **fork/pipe/execveの未解決flakyハングの根本原因特定**: `PHASE7D_HANDOFF.md`に記録された調査を
   引き継ぎ、シリアル診断が問題を隠してしまう(タイミングが変わる)制約があるため、QEMU monitor /
   gdbstub 経由で診断なしに停止箇所を捕まえる調査が必要。Phase 8(シグナル配送)以降で状況が変わって
   いる可能性があるため、まず現状の再現性を確認するところから。
3. **パニック診断のディスク永続化**: シリアル/VGAへの出力に加えて、簡易的にでもFAT32へパニック理由・
   file:line・レジスタダンプを書き出す(たとえば固定パスへの追記、または生セクタへの直接書き込み)。
   シリアルを繋いでいない状態での事後調査を可能にする。

### Phase B — シェルとファイル操作を実用レベルに(目安3〜5週間)

4. **ネイティブシェルにパイプ/リダイレクト/変数/簡易制御構文**: `src/shell.rs`の`tokenize`/`execute`を
   拡張し、`|`・`>`/`<`・`$VAR`・簡単な`if`/`for`をサポート。Linux ABI側(busybox sh)に頼らず、
   MachaOS自身のシェルで日常的なコマンド連携ができるようにする。
5. **`cp`の実装**: `fat.rs`にコピー関数を追加し、シェルの`cp`コマンドとファイルエクスプローラーの
   コピー&ペースト(Ctrl+C/Ctrl+V)の両方から使えるようにする。
6. **テキスト選択モデルの導入**: メモ帳・ターミナルに共通で使える範囲選択(ドラッグ・Shift+矢印)を
   実装し、Ctrl+C/X/Vを行単位ではなく選択範囲単位で動くようにする。ターミナルにもコピー機能を追加。

### Phase C — メモリ上限の引き上げ(目安3〜5日)

7. **物理メモリ上限を4GiBから拡張**: `src/paging.rs`の`IDENTITY_MAP_END`と`src/pmm.rs`の
   `MAX_PHYS_MEM`を、たとえば16〜32GiB相当まで引き上げる。動的なデマンドページングは不要で、
   起動時に確保するページテーブル・ビットマップを大きくするだけの、比較的閉じた変更。VMに
   多めのメモリを割り当てても無駄にならないようにする。

### Phase D — QoL(目安3〜4週間)

8. **ACPI S5(疑似)シャットダウン**: フルのAML(ACPI Machine Language)インタプリタは不要で、
   RSDP/FADTを読んで`PM1a_CNT`へ`SLP_TYP`/`SLP_EN`を書き込む最小限の実装で、VM上でも実際に
   電源が落ちるようにする。
9. **最小限のPNGデコーダ**: 画像ビューアがPNG(仕様が明確でスコープが閉じている)を直接開けるように
   する。JPEGは可変長・非可逆圧縮でスコープが大きく膨らむため後回し推奨。
10. **WM層への自動テスト追加**: 今回のUI修正セッションのように、QEMUのheadless起動+`screendump`+
    ピクセル比較を使ったスクリーンショット差分テストを1〜2本、`make test`に組み込む。GUIリグレッション
    を今後は手作業に頼らず検知できるようにする。

### Phase E — 任意の伸びしろ(余力があれば)

- **AC97オーディオ**: QEMU/UTMが標準でエミュレートしており、HDAより実装がシンプル。
- **ウィンドウスナップ / 仮想デスクトップ**
- **通知システム**
- **セッション復元の内容保持**(位置情報だけでなく未保存テキストなども復元)

---

## 4. 明示的にスコープ外(見落としではなく意図的な除外)

- **Webブラウザ**: ユーザー指示により対象外。
- **Linux互換層のさらなる拡張**: 既存の`linux_abi.rs`(シスコール表の15〜20%程度)は現状把握のみに
  留め、戦略的な拡張目標にはしない。
- **マルチユーザー/ログイン画面**: 個人の普段使いが目的である以上、単一ユーザー前提のままで
  実害は無いと判断。パスワードロックだけ欲しい場合はPhase E後の検討事項。

## 5. 将来課題として切り出す — 実機ブートアップ

以下は「いつかやる」将来プロジェクトとして名前だけ残す。それぞれ単体でも数週間〜数ヶ月級の
規模があり、全部合わせると今回のロードマップとは別に6ヶ月〜1年以上を要する見込み。今回は
工程化・スケジューリングしない。

- **UEFI起動パス**: 現状のBIOS/Multiboot1トランポリン(`src/main.rs`の手書きアセンブリ)とは別に、
  EFIスタブないしMultiboot2対応GRUBのUEFI版を用意する必要がある。GPT/ESPの取り扱いも必要。
- **USB HIDスタック**: xHCI/EHCI/UHCI/OHCIコントローラドライバ + USB HIDクラスドライバ + ハブ列挙。
  実機でキーボード/マウスを使うための必須条件だが、まるごと新規サブシステム。
- **NVMeドライバ**: PCIe経由のNVMeコマンドキュー管理。AHCIより新しいプロトコルで、実装量はAHCIの
  時と同程度以上。
- **ACPIのフル対応**: S5シャットダウンだけでなく、バッテリー残量・サーマル・C-state/P-stateなど。
  MADT経由のCPU/IOAPIC列挙はSMP対応の前提条件でもある。
- **SMP(マルチコア)対応**: Local APIC/IOAPICドライバ、APトランポリン、CPUごとのGDT/TSS/IDTスタック、
  そして現状「シングルコア前提」で書かれている全てのロック(`src/task.rs`のコメントが明言している)の
  再監査。

---

## 付録: このドキュメントの作り方

`src/`配下の全モジュールを3つの観点(ハードウェア/ドライバ、OSコア/ネットワーク/ファイルシステム/
プロセス、デスクトップ/アプリ)に分けて並列調査し、既存のハンドオフ文書(`PHASE7D_HANDOFF.md`)や
モジュール自身のドキュメントコメントを一次情報として突き合わせた上でまとめた。個々の主張には
可能な限り `file:line` の裏付けを付けてある(本文中の括弧書きを参照)。
